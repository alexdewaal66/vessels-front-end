// import { render, screen } from '@testing-library/react';
// import App from './App';

// test('<App /> renders Vessels title', () => {
//   render(<App />);
//   const element = screen.getByText(/Vessels/i);
//   expect(element).toBeInTheDocument();
// });

test('tests are running', () => {
  expect(true).toBe(true);
});